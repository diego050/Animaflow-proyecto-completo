# Environment
ENV=development

# OpenCode API (MiniMax M2.5)
OPENCODE_API_KEY=sk-MaWydCKyultji5UnP7gcGw0UMVpNmsrdrFIi61FzlR1bT0pKTanIArrlX3PtE38S
OPENCODE_MODEL=minimax-m2.5
OPENCODE_BASE_URL=https://api.opencode.ai/v1

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/animaflow

# Redis
REDIS_URL=redis://localhost:6379

# Voicebox (TTS)
VOICEBOX_URL=http://127.0.0.1:17493

# Storage
STORAGE_PATH=./storage

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Security (REQUIRED for production)
# Generate a strong SECRET_KEY for JWT signing
SECRET_KEY=change-this-in-production-to-a-random-string
# Generate ENCRYPTION_KEY with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
